"""Giai đoạn 2 — Full duplex "thật" + cảm xúc (docs/roadmap.md mục 2).

Các mảnh trong package này CHƯA chạy thử bằng hội thoại thật (cần ANTHROPIC_API_KEY +
tự nói/ngắt lời để quan sát) — xem docstring từng file để biết phần nào verify bằng
cách đọc source Pipecat 0.0.108 thật (không đoán API) và phần nào còn là heuristic tạm
thời chờ thay bằng model thật.
"""
