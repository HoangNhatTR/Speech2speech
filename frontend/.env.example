# Sao chép thành ".env" nếu Gateway không chạy ở localhost:7860 mặc định.
# URL/API key cũng đổi được ngay trong UI (nút "Kết nối gateway" ở header), lưu vào
# localStorage — biến này chỉ set giá trị khởi tạo ban đầu.
VITE_GATEWAY_URL=http://localhost:7860
