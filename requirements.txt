pipecat-ai[anthropic,deepgram,elevenlabs,silero,webrtc,runner,openai]==0.0.108
python-dotenv
nats-py
ray
sherpa-onnx

# eval harness (Track A)
jiwer
gTTS
soundfile
soxr
# Official Kyutai Moshi websocket protocol uses Opus via sphn. This is lightweight;
# torch/model weights remain isolated in .venv-s2s.
sphn==0.2.1

# datagen (Giai đoạn 3 — chuẩn bị dữ liệu, xem datagen/synthetic_dialogue.py)
numpy
